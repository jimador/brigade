---
type: llm
---

PASS if the response names the assigned ticket "Rate limit the login endpoint" (or its id
rate-limit-login) as the work on the board and either starts brigade's research/decomposition
for it or asks the operator which ticket to cook.
FAIL if the response implements code changes itself, invents tickets that are not on the
board, or ignores the board entirely.
